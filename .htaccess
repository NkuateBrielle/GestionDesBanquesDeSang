<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /GestionDesBanquesDeSang/
    
    # Redirect to public folder
    RewriteRule ^$ public/ [L]
    RewriteRule (.*) public/$1 [L]
</IfModule>